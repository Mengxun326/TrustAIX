# IdP response needs: active, sub, trustaix_tenant (or tenant_id), trustaix_role (or role).
TRUSTAIX_OIDC_INTROSPECTION_URL=https://idp.example.com/oauth2/introspect
TRUSTAIX_OIDC_CLIENT_ID=trustaix-gateway
TRUSTAIX_OIDC_CLIENT_SECRET=replace-from-secret-store
